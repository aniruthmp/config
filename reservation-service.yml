server:
  port: ${PORT:8000}

message: Hello Ani!

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: reservation-service
    leaseRenewalIntervalInSeconds: 10

ribbon:
  eureka:
    enabled: true